dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.42  # This one is already V2
  audio_session: ^0.1.24  # <-- ADD THIS LINE. just_audio needs it for V2
  file_picker: ^10.2.0
  permission_handler: ^11.4.0
