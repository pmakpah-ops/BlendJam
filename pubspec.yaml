name: blendjam
description: A new Flutter project for music.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audio_session: ^0.2.8
  just_audio: ^0.9.40

flutter:
  uses-material-design: true
