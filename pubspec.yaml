name: blendjam
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.5.0   # <-- ADD THIS LINE. This version has FilePickerResult + FileType
  
  # Add your other packages here
  # just_audio: ^0.9.42
  # provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
